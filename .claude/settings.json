{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path') && case \"$FILE\" in *.rs) rustfmt --quiet \"$FILE\" 2>/dev/null || true ;; esac"
          }
        ]
      }
    ]
  }
}
