# Temporary files
*.000
*.bak
*.bck
*.kicad_pcb-bak
*.kicad_sch-bak
*-backups
*.kicad_prl
*.sch-bak
*~
_autosave-*
*.tmp
*-save.pro
*-save.kicad_pcb
fp-info-cache
~*.lck
\#auto_saved_files#

# Netlist files (exported from Eeschema)
*.net

# Autorouter files (exported from Pcbnew)
*.dsn
*.ses

# Exported BOM files
*.xml
*.csv

# Rust / Cargo build output directory
build/
target/

# Nix build output symlinks
result
result-*

# Nix dev shell local toolchains (not root-anchored — applies at any depth,
# e.g. boards/dummy-button/.cargo/ created by espup/nix dev shells).
# Already-tracked files like firmware/.cargo/config.toml are unaffected.
.cargo/
.rustup/
